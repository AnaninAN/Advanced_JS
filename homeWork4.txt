const str = '...'; //Ѕольшой текст
const regexp = /^'|\s'|'\s|'$/g; //Ўаблон, который находит одинарные кавычки, за исключением где они наход€тс€ внутри слова
str.replace(regexp,'"'); //«амен€ет одинарные кавычки на двойные, согласно шаблону 